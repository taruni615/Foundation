# Development / CI tooling. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0        # test runner; suite lives in tests/
ruff>=0.6          # linter (pyflakes + bugbear + import sorting), config in pyproject.toml
black>=24.0        # formatter
isort>=5.13        # import sorting (black profile)
mypy>=1.11         # type checking for shared/ repository/ ai/
pip-audit>=2.7     # dependency vulnerability scanning
